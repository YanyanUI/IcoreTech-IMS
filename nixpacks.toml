[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo", "php83Extensions.pdo_pgsql"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
```

Then update your **`Procfile`** to just this:
```
web: php -S 0.0.0.0:$PORT -t .